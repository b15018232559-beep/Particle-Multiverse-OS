# Workspace Memory Plugin

Stores workspace state, scene analytics, and preferences locally. It supports enable, disable, export, and import through the Plugin System. It does not upload data or read private files, browser history, or chat content.
